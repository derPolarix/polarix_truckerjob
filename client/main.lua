local debug = require("shared.debug")
local client = require("config.client")
local shared = require("shared.debug")

-- AddEventHandler('onResourceStart', function(resourceName)
--     if (GetCurrentResourceName() ~= resourceName) then
--         return
--     end
-- end)

-- AddEventHandler('onResourceStop', function(resourceName)
--     if (GetCurrentResourceName() ~= resourceName) then
--         return
--     end
-- end)

-- Funktion mit Beispielen die Häufig verwendet werden

-- function ExampleFunction()
--     -- Clientseitig abrufen der PlayerData vom Spieler die immer aktuell ist ohne Server anzufragen
--     -- PlayerData type: https://docs.qbox.re/resources/qbx_core/types/player#playerdata
--     local Player = QBX.PlayerData
-- end
